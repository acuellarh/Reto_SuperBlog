<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm-6">        
        <%= form_for (post) do |f| %>
        <% if post.errors.any? %>
          <div class="alert alert-danger" role="alert">
            <h5> Lo siguiente prohibe que este post sea guardado: </h5>            
            <ul>        
              <% post.errors.full_messages.each do |message| %>
              <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
      
        <h1><%= form_title %></h1>

          <div class="form-group">  
            <%= f.text_field :title, class: "form-control input-lg",
            placeholder: "Ingrese el titulo del Post"%>
          </div>
          <div class="form-group">
            <%= f.text_area :content, class: "form-control input-lg",
            placeholder: "Ingrese el contenido del Post, mínimo 250 caracteres" %>
          </div>           
          <div>
            <%= f.submit form_title , class: "btn btn-primary btn-lg btn-block" %>
          </div>
      
      <% end %>
      
      
    </div>
  </div>
</div>



